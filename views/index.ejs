<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Breaking Waves</title>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,700,700i|Permanent+Marker&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Lora:400,400italic,700,700italic" rel="stylesheet" type="text/css"/>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet"/>
    <script>
      <%- ctx ? "window.prismic = { endpoint: '" + ctx.endpoint + "' };" : "" -%>
      <% var maybeCurrentExperiment = ctx.api.currentExperiment(); %>
      <% if (maybeCurrentExperiment) { %>
        <% var googleId = maybeCurrentExperiment.googleId() %>
        window.googleId = "<%- googleId -%>";
      <% } %>
    </script>
    <script src="//static.cdn.prismic.io/prismic.min.js"></script>
    <script>PrismicToolbar.startExperiment(window.googleId);</script>
  </head>
  <body>
    <div id="main"><%- markup -%></div>
    <script>
      window.__PRISMIC__ = <%- JSON.stringify(PRISMIC_UNIVERSAL_DATA) -%>;
    </script>
    <script src="/js/compiled/client.js"></script>
  </body>
</html>
